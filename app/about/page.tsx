import type { Metadata } from "next";
import Link from "next/link";
import { PageShell } from "@/components/site-shell";
import { getPublishedManifest } from "@/lib/content";
export const metadata: Metadata = { title: "About", description: "About Sendil and the thinking behind Labs Built by Sendil." };
export default async function AboutPage() { const data = await getPublishedManifest(); return <PageShell site={data.site}><section className="about-hero section-pad"><div className={data.site.portraitUrl ? "profile-object has-photo" : "profile-object"} style={data.site.portraitUrl ? { backgroundImage: `url(${data.site.portraitUrl})` } : undefined} aria-label={data.site.portraitUrl ? "Portrait of Sendil" : "Sendil monogram"}><span>{data.site.portraitUrl ? "" : "S"}</span></div><div><span className="eyebrow">About Sendil</span><h1>Architecture is really the practice of seeing the whole.</h1><p className="hero-lede">{data.site.biography}</p></div></section><section className="beliefs section-pad"><span className="eyebrow">A multi-faceted point of view</span><div className="belief-grid"><article><span>01</span><h2>Technology has a context.</h2><p>A sound solution fits the people, constraints, timing, and purpose around it—not only the diagram.</p></article><article><span>02</span><h2>Clarity creates momentum.</h2><p>Useful architecture makes decisions easier to understand, test, and change as the situation evolves.</p></article><article><span>03</span><h2>Experience should travel.</h2><p>Writing, speaking, and mentoring turn lessons from one room into progress in another.</p></article></div></section><section className="about-audiences section-pad"><div><span className="eyebrow">Who this lab is for</span><h2>People building their next capability.</h2></div><p>Students taking their first technical steps. Professionals navigating greater complexity. Business leaders connecting intent with execution. Founders making consequential choices with finite time and resources.</p></section><section className="closing-cta section-pad"><p className="manifesto-mark" aria-hidden="true">S</p><div><span className="eyebrow">A useful conversation can change the shape of a problem.</span><h2>What are you trying to make possible?</h2><Link href="/advisory/" className="button">Send an inquiry ↗</Link></div></section></PageShell>; }
